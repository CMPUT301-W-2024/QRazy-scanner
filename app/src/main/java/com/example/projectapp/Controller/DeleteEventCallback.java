package com.example.projectapp.Controller;

/**
 * An interface defining a callback mechanism to signal the delete of an event.
 */
public interface DeleteEventCallback {
    void onDeleteEvent(String eventId);
}
