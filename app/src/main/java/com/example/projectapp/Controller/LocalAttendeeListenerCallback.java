package com.example.projectapp.Controller;

/**
 * An interface defining a callback mechanism to signal the delete of a local attendee.
 */
public interface LocalAttendeeListenerCallback {
    void onLocalAttendeeDeleted();
}
