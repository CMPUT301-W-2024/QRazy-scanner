package com.example.projectapp.Controller;

/**
 * An interface defining a callback mechanism to signal the update of an event.
 */
public interface UpdateEventCallback {
    void onUpdateEvent(String eventId);
}
